# Создание таблицы post_counter пошагово 🎯

## Шаг 1: Откройте консоль Яндекс Облака

1. Перейдите на https://console.cloud.yandex.ru/
2. Слева в меню найдите **YDB** или **Управляемая база данных**
3. Нажмите на вашу базу данных (где уже есть таблицы orders, additional_invoices и т.д.)

## Шаг 2: Откройте редактор запросов

1. В открытой базе нажмите на вкладку **"Запросы"** или **"Query"** (вверху)
2. Откроется редактор кода с белым полем для ввода
3. Очистите поле, если там что-то было

## Шаг 3: Вставьте правильный YQL запрос

Копируйте ЭТОТ запрос (именно так, по символам):

```yql
CREATE TABLE post_counter (
    key Utf8 NOT NULL,
    text_index Int32,
    image_index Int32,
    PRIMARY KEY (key)
);
```

## Шаг 4: Выполните запрос

1. Нажмите кнопку **"Выполнить"** (Run) - обычно она внизу справа или сверху
2. Подождите 2-3 секунды
3. Внизу должно появиться сообщение **"OK"** или **"Успешно"**

## Шаг 5: Проверьте что таблица создалась

В левой панели найдите раздел **"Таблицы"** и проверьте, что там появилась новая таблица `post_counter`

---

## ✅ Если всё прошло успешно:

Таблица готова! Теперь система автопостинга будет работать правильно.

## ❌ Если ошибка:

### Ошибка: "Table with same name already exists"
- Таблица уже есть в базе - это хорошо! Просто используйте её.

### Ошибка: "Syntax error"
- Скопируйте запрос еще раз, убедитесь что нет опечаток
- Обычно в консоли показано где ошибка

### Другая ошибка:
- Скопируйте полный текст ошибки и напишите мне - я помогу

---

## 📝 Что делает эта таблица?

- **key** - уникальный ключ (всегда будет "global")
- **text_index** - номер текущей темы для текста (0-7)
- **image_index** - номер текущей темы для изображения (0-7)

Система будет автоматически обновлять эти цифры при каждом посте!
